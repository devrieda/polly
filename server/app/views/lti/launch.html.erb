<div id='polly'></div>

<%# We can remove the following once we are underway. printing out the launch
    params so we are aware of what is available %>
<%= image_tag 'polly.png' %>
<ul>
  <% @launch_params.keys.each do |k| %>
    <li><%= k %>: <%= @launch_params[k]%></li>
  <% end %>
</ul>
